# Plan: RayNeo X3 Mercury adaptation

## Tasks

1. Wire vendor AAR + ViewBinding + stubs for CI without AAR
2. Remove phone camera underlay / phone scale from web activity
3. `MobileWebActivity` → `BaseMirrorActivity` dual WebView
4. Temple → JS `__bitfunTemple` + web `[data-rayneo-focus]`
5. Force RayNeo voice PCM path
6. README + compile/unit tests
