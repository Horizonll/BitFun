<?xml version="1.0" encoding="utf-8"?>
<!-- Optical see-through: no camera preview, no aiming frame — title only. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bf_black">

    <TextView
        android:id="@+id/scanHint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="@string/scan_title"
        android:textColor="@color/bf_white"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Kept for BindingPair / permission flow; hidden unless camera access is needed. -->
    <TextView
        android:id="@+id/scanStatus"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <Button
        android:id="@+id/permissionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="20dp"
        android:background="@android:color/transparent"
        android:minWidth="0dp"
        android:minHeight="0dp"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        android:text="@string/scan_grant_camera"
        android:textAllCaps="false"
        android:textColor="@color/bf_white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:visibility="gone" />
</FrameLayout>
